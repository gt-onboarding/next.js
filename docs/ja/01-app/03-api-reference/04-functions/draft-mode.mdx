---
title: draftMode
description: draftMode 関数の API リファレンス。
related:
  title: 次のステップ
  description: このステップバイステップガイドで Draft Mode の使い方を学びます。
  links:
    - app/guides/draft-mode
---

`draftMode` は、[Draft Mode](/docs/app/guides/draft-mode) を有効または無効にでき、さらに [Server Component](/docs/app/getting-started/server-and-client-components) 内で Draft Mode が有効かどうかを確認できる **非同期** 関数です。

```tsx filename="app/page.ts" switcher
import { draftMode } from 'next/headers'

export default async function Page() {
  const { isEnabled } = await draftMode()
}
```

```jsx filename="app/page.js" switcher
import { draftMode } from 'next/headers'

export default async function Page() {
  const { isEnabled } = await draftMode()
}
```

## リファレンス

次のメソッドとプロパティを使用できます:

| メソッド    | 説明                                                                                   |
| ----------- | -------------------------------------------------------------------------------------- |
| `isEnabled` | Draft Mode が有効かどうかを示すブール値です。                                          |
| `enable()`  | Cookie（`__prerender_bypass`）を設定して、Route Handler で Draft Mode を有効にします。 |
| `disable()` | Cookie を削除して、Route Handler で Draft Mode を無効にします。                        |

## 補足

- `draftMode` は **非同期** 関数で、Promise を返します。`async/await` または React の [`use`](https://react.dev/reference/react/use) 関数を使用する必要があります。
  - バージョン 14 以前では、`draftMode` は同期関数でした。後方互換性を保つため、Next.js 15 でも同期的にアクセスできますが、この挙動は将来的に非推奨になる予定です。
- 新しいバイパス Cookie の値は、`next build` を実行するたびに生成されます。これにより、バイパス Cookie が推測されることを防ぎます。
- HTTP 経由のローカル環境で Draft Mode をテストするには、ブラウザでサードパーティ Cookie とローカルストレージへのアクセスを許可する必要があります。

## 使用例

### Draft Mode を有効化する

Draft Mode を有効にするには、新しい [Route Handler](/docs/app/api-reference/file-conventions/route) を作成して、`enable()` メソッドを呼び出します。

```tsx filename="app/draft/route.ts" switcher
import { draftMode } from 'next/headers'

export async function GET(request: Request) {
  const draft = await draftMode()
  draft.enable()
  return new Response('Draft mode is enabled')
}
```

```js filename="app/draft/route.js" switcher
import { draftMode } from 'next/headers'

export async function GET(request) {
  const draft = await draftMode()
  draft.enable()
  return new Response('Draft mode is enabled')
}
```

### Draft Mode を無効化する

デフォルトでは、ブラウザを閉じると Draft Mode のセッションは終了します。

Draft Mode を手動で無効化するには、[Route Handler](/docs/app/api-reference/file-conventions/route) 内で `disable()` メソッドを呼び出します。

```tsx filename="app/draft/route.ts" switcher
import { draftMode } from 'next/headers'

export async function GET(request: Request) {
  const draft = await draftMode()
  draft.disable()
  return new Response('Draft mode is disabled')
}
```

```js filename="app/draft/route.js" switcher
import { draftMode } from 'next/headers'

export async function GET(request) {
  const draft = await draftMode()
  draft.disable()
  return new Response('Draft mode is disabled')
}
```

次に、Route Handler を呼び出すためのリクエストを送信します。[`<Link>` component](/docs/app/api-reference/components/link) を使ってそのルートを呼び出す場合、プリフェッチ時にクッキーが誤って削除されてしまうのを防ぐため、必ず `prefetch={false}` を渡す必要があります。

### Draft Mode が有効かどうかの確認

Server Component 内では、`isEnabled` プロパティを使って Draft Mode が有効になっているかどうかを確認できます。

```tsx filename="app/page.ts" switcher
import { draftMode } from 'next/headers'

export default async function Page() {
  const { isEnabled } = await draftMode()
  return (
    <main>
      <h1>My Blog Post</h1>
      <p>Draft Mode is currently {isEnabled ? 'Enabled' : 'Disabled'}</p>
    </main>
  )
}
```

```jsx filename="app/page.js" switcher
import { draftMode } from 'next/headers'

export default async function Page() {
  const { isEnabled } = await draftMode()
  return (
    <main>
      <h1>My Blog Post</h1>
      <p>Draft Mode is currently {isEnabled ? 'Enabled' : 'Disabled'}</p>
    </main>
  )
}
```

## バージョン履歴

| バージョン   | 変更点                                                                                                                  |
| ------------ | ----------------------------------------------------------------------------------------------------------------------- |
| `v15.0.0-RC` | `draftMode` が非同期関数 (async 関数) になりました。[codemod](/docs/app/guides/upgrading/codemods#150) が利用可能です。 |
| `v13.4.0`    | `draftMode` が導入されました。                                                                                          |
