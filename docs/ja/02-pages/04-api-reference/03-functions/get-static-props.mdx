---
title: getStaticProps
description: '`getStaticProps` の API リファレンス。Next.js で `getStaticProps` を使って静的ページを生成する方法について説明します。'
---

`getStaticProps` という名前の関数をエクスポートすると、その関数が返す props を使ってビルド時にページが事前レンダリングされます。

```tsx filename="pages/index.tsx" switcher
import type { InferGetStaticPropsType, GetStaticProps } from 'next'

type Repo = {
  name: string
  stargazers_count: number
}

export const getStaticProps = (async (context) => {
  const res = await fetch('https://api.github.com/repos/vercel/next.js')
  const repo = await res.json()
  return { props: { repo } }
}) satisfies GetStaticProps<{
  repo: Repo
}>

export default function Page({
  repo,
}: InferGetStaticPropsType<typeof getStaticProps>) {
  return repo.stargazers_count
}
```

```jsx filename="pages/index.js" switcher
export async function getStaticProps() {
  const res = await fetch('https://api.github.com/repos/vercel/next.js')
  const repo = await res.json()
  return { props: { repo } }
}

export default function Page({ repo }) {
  return repo.stargazers_count
}
```

トップレベルスコープでモジュールをインポートして、`getStaticProps` 内で使用できます。インポートしたモジュールは**クライアントサイドのバンドルには含まれません**。これは、データベースからのデータ取得を含めて、**サーバーサイドのコードを `getStaticProps` 内に直接記述できる**ことを意味します。

## Context パラメーター

`context` パラメーターは、次のキーを含むオブジェクトです。

| 名前               | 説明                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `params`           | [動的ルート](/docs/pages/building-your-application/routing/dynamic-routes)を使用するページのルートパラメーターを含みます。たとえば、ページ名が `[id].js` の場合、`params` は `{ id: ... }` のようになります。これは後述する `getStaticPaths` と組み合わせて使用する必要があります。                                                                                                                                                  |
| `preview`          | (`draftMode` では非推奨) ページが[プレビューモード](/docs/pages/guides/preview-mode)の場合は `preview` が `true`、それ以外の場合は `false` です。                                                                                                                                                                                                                                                                                    |
| `previewData`      | (`draftMode` では非推奨) `setPreviewData` によって設定された[プレビュー](/docs/pages/guides/preview-mode)データです。                                                                                                                                                                                                                                                                                                                |
| `draftMode`        | ページが[ドラフトモード](/docs/pages/guides/draft-mode)の場合は `draftMode` が `true`、それ以外の場合は `false` です。                                                                                                                                                                                                                                                                                                               |
| `locale`           | 有効化されている場合、アクティブなロケールを含みます。                                                                                                                                                                                                                                                                                                                                                                               |
| `locales`          | 有効化されている場合、サポートされているすべてのロケールを含みます。                                                                                                                                                                                                                                                                                                                                                                 |
| `defaultLocale`    | 有効化されている場合、設定されているデフォルトロケールを含みます。                                                                                                                                                                                                                                                                                                                                                                   |
| `revalidateReason` | 関数が呼び出された理由を表します。次のいずれかです: "build" (ビルド時に実行)、"stale" (再検証期間が期限切れになった場合、または[開発モード](/docs/pages/building-your-application/data-fetching/get-static-props#runs-on-every-request-in-development)で実行される場合)、"on-demand" ([オンデマンド再検証](/docs/pages/guides/incremental-static-regeneration#on-demand-revalidation-with-revalidatepath)によってトリガーされた場合) |

## getStaticProps の返り値

`getStaticProps` 関数は、`props`、`redirect`、または `notFound` のいずれかを含み、さらに **任意** の `revalidate` プロパティを持つオブジェクトを返す必要があります。

### `props`

`props` オブジェクトはキーと値のペアからなるオブジェクトであり、それぞれの値はページコンポーネントによって受け取られます。渡される任意の props が [`JSON.stringify`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify) でシリアライズ可能となるよう、[シリアライズ可能なオブジェクト](https://developer.mozilla.org/docs/Glossary/Serialization) である必要があります。

```jsx
export async function getStaticProps(context) {
  return {
    props: { message: `Next.js is awesome` }, // ページコンポーネントにpropsとして渡されます
  }
}
```

### `revalidate`

`revalidate` プロパティは、ページを再生成できるようになるまでの秒数を指定します（デフォルトは `false` で、再検証は行われません）。

```js
// この関数はサーバーサイドのビルド時に呼び出されます。
// 再検証が有効で新しいリクエストが来た場合、
// サーバーレス関数上で再度呼び出される可能性があります
export async function getStaticProps() {
  const res = await fetch('https://.../posts')
  const posts = await res.json()

  return {
    props: {
      posts,
    },
    // Next.js はページの再生成を試みます:
    // - リクエストが来たとき
    // - 最大で10秒に1回
    revalidate: 10, // 秒単位
  }
}
```

[Incremental Static Regeneration](/docs/pages/guides/incremental-static-regeneration) について詳しくは参照してください。

ISR を利用するページのキャッシュステータスは、レスポンスヘッダー `x-nextjs-cache` の値を確認することで判別できます。取りうる値は次のとおりです:

- `MISS` - パスがキャッシュに存在しない（初回アクセス時に一度だけ発生）
- `STALE` - パスはキャッシュに存在するが、`revalidate` 時間を超過しているためバックグラウンドで更新される
- `HIT` - パスはキャッシュに存在し、`revalidate` 時間を超過していない

### `notFound`

`notFound` ブール値を使用すると、ページで `404` ステータスと [404 Page](/docs/pages/building-your-application/routing/custom-error#404-page) を返すことができます。`notFound: true` を指定すると、以前にページが正常に生成されていた場合でも、そのページは `404` を返します。これは、ユーザー生成コンテンツが作者によって削除されるといったユースケースをサポートすることを意図しています。なお、`notFound` は [ここで説明されている](#revalidate) `revalidate` と同じ挙動に従います。

```js
export async function getStaticProps(context) {
  const res = await fetch(`https://.../data`)
  const data = await res.json()

  if (!data) {
    return {
      notFound: true,
    }
  }

  return {
    props: { data }, // ページコンポーネントにpropsとして渡される
  }
}
```

> **補足**: [`fallback: false`](/docs/pages/api-reference/functions/get-static-paths#fallback-false) モードでは、`notFound` は不要です。`getStaticPaths` から返されたパスのみが事前レンダリングされるためです。

### `redirect`

`redirect` オブジェクトを使用すると、内部・外部のリソースにリダイレクトできます。これは `{ destination: string, permanent: boolean }` という型に従う必要があります。

まれに、古い `HTTP` クライアントが正しくリダイレクトできるように、カスタムステータスコードを割り当てる必要がある場合があります。そのような場合は、`permanent` プロパティの代わりに `statusCode` プロパティを使用できますが、**両方を同時に使用することはできません**。また、`next.config.js` のリダイレクトと同様に、`basePath: false` を設定することもできます。

```js
export async function getStaticProps(context) {
  const res = await fetch(`https://...`)
  const data = await res.json()

  if (!data) {
    return {
      redirect: {
        destination: '/',
        permanent: false,
        // statusCode: 301
      },
    }
  }

  return {
    props: { data }, // ページコンポーネントにpropsとして渡される
  }
}
```

リダイレクトがビルド時に既知である場合は、代わりに [`next.config.js`](/docs/pages/api-reference/config/next-config-js/redirects) に追加してください。

## ファイルの読み取り: `process.cwd()` を使用する

`getStaticProps` 内では、ファイルシステムからファイルを直接読み取ることができます。

そのためには、ファイルの絶対パスを取得する必要があります。

Next.js はコードを別のディレクトリにコンパイルするため、`__dirname` をパスとして使用することはできません。返されるパスが Pages Router の場合とは異なるためです。

代わりに、Next.js が実行されているディレクトリを返す `process.cwd()` を使用できます。

```jsx
import { promises as fs } from 'fs'
import path from 'path'

// posts はビルド時に getStaticProps() によって設定されます
function Blog({ posts }) {
  return (
    <ul>
      {posts.map((post) => (
        <li>
          <h3>{post.filename}</h3>
          <p>{post.content}</p>
        </li>
      ))}
    </ul>
  )
}

// この関数はサーバーサイドのビルド時に呼び出されます。
// クライアントサイドでは呼び出されないため、
// データベースへの直接クエリも実行できます。
export async function getStaticProps() {
  const postsDirectory = path.join(process.cwd(), 'posts')
  const filenames = await fs.readdir(postsDirectory)

  const posts = filenames.map(async (filename) => {
    const filePath = path.join(postsDirectory, filename)
    const fileContents = await fs.readFile(filePath, 'utf8')

    // 通常はここでコンテンツを解析・変換します
    // 例えば、マークダウンを HTML に変換できます

    return {
      filename,
      content: fileContents,
    }
  })
  // { props: { posts } } を返すことで、Blog コンポーネントは
  // ビルド時に `posts` を props として受け取ります
  return {
    props: {
      posts: await Promise.all(posts),
    },
  }
}

export default Blog
```

## バージョン履歴

| バージョン | 変更点                                                                                                                                                                    |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `v13.4.0`  | [App Router](/docs/app/getting-started/fetching-data) が安定版になり、データ取得が簡素化されました。                                                                      |
| `v12.2.0`  | [On-Demand Incremental Static Regeneration](/docs/pages/guides/incremental-static-regeneration#on-demand-revalidation-with-revalidatepath) が安定版になりました。         |
| `v12.1.0`  | [On-Demand Incremental Static Regeneration](/docs/pages/guides/incremental-static-regeneration#on-demand-revalidation-with-revalidatepath) が追加されました（ベータ版）。 |
| `v10.0.0`  | `locale`、`locales`、`defaultLocale`、`notFound` オプションが追加されました。                                                                                             |
| `v10.0.0`  | 戻り値のオプション `fallback: 'blocking'` が追加されました。                                                                                                              |
| `v9.5.0`   | [Incremental Static Regeneration](/docs/pages/guides/incremental-static-regeneration) が安定版になりました。                                                              |
| `v9.3.0`   | `getStaticProps` が導入されました。                                                                                                                                       |
